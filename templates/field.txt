<%page args="field"/>
{{:Web2Cit/monitor/log/field
|fieldname = ${field.get('name', '')}
|expected =
    <table class="wikitable">
    % for test_field in field.get('test', []):
        <tr>
            <td>${test_field}</td>
        </tr>
        % endfor
    </table>
|actual =
     <table class="wikitable">
        % for output_field in field.get('output', []):
        <tr>
            <td>${output_field}</td>
        </tr>
        % endfor
    </table>
|score = ${round(field.get('score', 0), 4)*100}%
|since = ?
}}